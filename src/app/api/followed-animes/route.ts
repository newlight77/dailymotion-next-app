export async function GET() {
  return new Response(JSON.stringify({ error: 'followed-animes feature removed' }), { status: 410 });
}

export async function POST() {
  return new Response(JSON.stringify({ error: 'followed-animes feature removed' }), { status: 410 });
}

export async function DELETE() {
  return new Response(JSON.stringify({ error: 'followed-animes feature removed' }), { status: 410 });
}
